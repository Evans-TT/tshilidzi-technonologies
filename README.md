# Tshilidzi Technologies Static Site

React + Vite rebuild of the Wix site, prepared for static deployment.

## Run Locally

```bash
npm install
npm run dev
```

## Build For GitHub Pages

```bash
npm run build
```

The deployable static site is generated in `dist/`.
